---
name: todolist-manager
description: Todolist 管理技能。支持新增、更新、查询 Todo，支持每日复盘自动更新。
author: 乖乖虾
version: 3.0.0
---

# Todolist Manager

## 一、核心能力

### 1. 新增 Todo

**触发：** 用户口述或对话分析发现新事项

**操作：**
- 状态：`待确认 (AI 提交)`
- 日志：记录来源（用户口述/对话分析）

**示例：**
```
用户：帮我记个 Todo，明天要研究朋友圈分析
→ 新增 Todo，状态=待确认 (AI 提交)
```

---

### 2. 更新 Todo

**可更新：**
- 状态：`进行中` / `待验收 (AI 提交)`
- 日志：AI 可直接写

**不可更新：** 优先级、截止日期、执行者、任务描述

**状态含义：**
| 状态 | 含义 | 触发条件 |
|------|------|---------|
| `进行中` | 用户正在推进 | 对话中看到用户在执行 |
| `待验收 (AI 提交)` | AI 判断已完成 | 用户明确表示完成，或产出已可用 |
| `待确认 (AI 提交)` | AI 建议新增 | 断线头/已完成未记录，需用户确认 |

**示例：**
```
用户：定时任务的 chat-logs 读取功能搞定了
→ 更新状态=待验收 (AI 提交)，追加日志
```

---

### 3. 查询 Todo

**支持：**
- 按状态筛选（进行中/待验收/未启动）
- 按优先级筛选（P1/P2/P3）

---

### 4. 创建新 Todolist

**触发：** 用户明确要求创建新 Todolist

**操作：**
1. 创建多维表格 App
2. 添加标准字段（任务名称、状态、日志、优先级等）
3. 描述中包含"todolist"关键词（便于发现）

---

## 二、触发场景

### 场景 A：日常对话触发

**特征：** 零散、随意、单点操作

**示例：**
- "帮我记个 Todo，明天要研究朋友圈分析"
- "更新一下定时任务的进展，已经可以读 chat-logs 了"
- "那个图片分析功能做完了，记一下"

**处理：** 即时响应，调用对应核心能力

---

### 场景 B：定时任务触发（每日复盘 SOP）

**特征：** 系统性、批量操作、自动化

**触发：** 定时任务消息："按照 <todolist-manager>技能，执行每日复盘"

**SOP 流程：**

#### Step 1: 读取 chat-logs

```bash
exec: powershell -ExecutionPolicy Bypass -File "C:\Users\44452\.openclaw\skills\todolist-manager\scripts\read-chatlogs.ps1" -Date "YYYY-MM-DD"
```

**注意：** 定时任务在早上 7:00 执行，读取**前一天**的对话

---

#### Step 2: 对话分析

**核心：** 为了更新 TodoList 而读，不是关键词匹配

**分析框架：**

**1. 这在 TodoList 里吗？**
- 在 → 判断状态变化
  - 完成了？→ 更新状态为`待验收 (AI 提交)`
  - 推进了？→ 追加日志
  - 堵住了？→ 追加日志（记录堵点）

- 不在 → 判断是否值得记录
  - 花了很多时间？（对话轮数≥3）
  - 完成了还是中断了？
  - 完成了 → 可能是更大任务的子任务，建议新增
  - 中断了 → 高优先级断线头，建议新增

**2. 这个事项的重要性？**
- 用户投入了多少时间？（对话轮数、时长）
- 是否反复提及？
- 是否与其他 Todo 有关联？

---

#### Step 3: 批量更新 TodoList

**新增 Todo：**
- 断线头：花了很多时间但中断的事项
- 已完成未记录：重要能力开发

**更新状态：**
- `进行中`：看到用户在对话中推进任务
- `待验收 (AI 提交)`：用户明确表示完成

**追加日志：**
- 记录进展、堵点、决策

---

#### Step 4: 生成本地报告

**保存路径：**
```
C:\Users\44452\.openclaw\agents\guaiguaixia\workspace\scratchpad\daily-brief-YYYY-MM-DD.md
```

**报告格式：**
```markdown
# 每日复盘 - YYYY 年 MM 月 DD 日

## 任务状态总览
- 总任务数：X
- 已完成：X
- 进行中：X
- 待启动：X

## 状态更新
1. 任务 A → 待验收 (AI 提交)
   依据：...

## 建议新增
1. 任务 B（已完成）
   建议优先级：2
   理由：...

## 断线头检测
1. 任务 C（中断）
   原因：...
   建议优先级：2

## 下一步建议
1. 验收任务 A（昨日完成）
2. 启动任务 C（昨日中断）
```

**注意：** 本地报告必须生成，推送由定时任务配置负责

---

## 三、配置

**当前维护的 Todolist：**
- 名称：🐉🦐龙虾养成计划
- Token: `Ms0Sbx75jahr5QsX4vQc8rnxn2f`
- 表格 ID: `tblU1u2KzTmZIqdw`

（可选，便于 AI 自我执行）

---

## 四、字段说明

**标准字段：**
| 字段名 | 类型 | AI 可更新 | 说明 |
|--------|------|---------|------|
| 任务名称 | 文本 | ✅ | 任务标题 |
| 任务描述 | 文本 | ❌ | 任务详情 |
| 任务状态 | 单选 | ✅ | 待确认/进行中/待验收等 |
| 任务日志 | 文本 | ✅ | 进展记录，AI 可追加 |
| 任务优先级 | 单选 | ❌ | P1/P2/P3 |
| 截止日期 | 日期 | ❌ | 用户设定 |
| 任务执行人 | 人员 | ❌ | 用户设定 |

---

_Updated: 2026-03-22_
_Version: 3.0 (重构：按触发场景组织，聚焦每日复盘 SOP)_
